import React from "react";import ReactDOM from "react-dom/client";import {useRegisterSW} from "virtual:pwa-register/react";import App from "./App";import "./styles.css";function Update(){const {needRefresh:[need],updateServiceWorker}=useRegisterSW();return need?<div className="update">Nova versão disponível <button onClick={()=>void updateServiceWorker(true)}>Atualizar</button></div>:null}ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><App/><Update/></React.StrictMode>);